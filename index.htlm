
<!doctype html>
<html lang="fr">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Coco Lonel — Restaurant</title>
  <meta name="description" content="Coco Lonel — Restaurant. Consultez le menu, les horaires et réservez." />

  <style>
    :root{
      /* Couleurs de la marque */
      --accent:#3d251f;   /* principal */
      --accent2:#714d42;  /* secondaire */

      --bg:#0b1220;
      --card:rgba(15,26,48,.72);
      --text:#eaf0ff;
      --muted:#b7c3e6;
      --border:rgba(255,255,255,.10);
      --shadow: 0 18px 50px rgba(0,0,0,.35);
    }
    *{box-sizing:border-box}
    body{
      margin:0;
      font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Arial;
      background:
        radial-gradient(1200px 700px at 20% 0%, rgba(61,37,31,.35), transparent 60%),
        radial-gradient(900px 600px at 80% 10%, rgba(113,77,66,.25), transparent 55%),
        var(--bg);
      color:var(--text);
    }
    a{color:inherit; text-decoration:none}
    .container{max-width:1100px; margin:0 auto; padding:22px}
    header{
      position:sticky; top:0; z-index:10;
      backdrop-filter: blur(10px);
      background: rgba(11,18,32,.70);
      border-bottom:1px solid var(--border);
    }
    .nav{display:flex; align-items:center; justify-content:space-between; gap:14px}
    .brand{display:flex; align-items:center; gap:12px; font-weight:850}
    .brandText{line-height:1}
    .brandText .name{font-size:16px}
    .brandText .tagline{font-size:13px; color:var(--muted); margin-top:4px}

    /* Logo */
    .logoWrap{
      width:54px; height:54px;
      border-radius:16px;
      padding:7px;
      background: linear-gradient(135deg, var(--accent), var(--accent2));
      box-shadow: 0 12px 26px rgba(0,0,0,.25);
      display:grid; place-items:center;
      border:1px solid rgba(255,255,255,.12);
      flex:0 0 auto;
    }
    .logoWrap img{
      width:100%; height:100%;
      object-fit:contain;
      border-radius:12px;
      background: rgba(255,255,255,.06);
    }

    .links{display:flex; gap:10px; flex-wrap:wrap}
    .links a{padding:10px 12px; border-radius:12px; color:var(--muted)}
    .links a:hover{background:rgba(255,255,255,.06); color:var(--text)}
    .btn{
      display:inline-flex; align-items:center; justify-content:center; gap:10px;
      padding:12px 16px; border-radius:14px;
      border:1px solid rgba(255,255,255,.14);
      background:rgba(255,255,255,.06);
      color:var(--text);
      box-shadow: 0 8px 22px rgba(0,0,0,.18);
      cursor:pointer;
    }
    .btn.primary{
      border:none;
      background: linear-gradient(135deg, var(--accent), var(--accent2));
      color:#fff;
      font-weight:900;
    }
    .btn:hover{transform: translateY(-1px)}
    section{scroll-margin-top:90px}
    .hero{padding:46px 0 16px}
    .hero-grid{display:grid; grid-template-columns: 1.25fr .75fr; gap:18px; align-items:stretch}
    @media (max-width:900px){ .hero-grid{grid-template-columns:1fr} }
    .card{
      background: var(--card);
      border:1px solid var(--border);
      border-radius:20px;
      box-shadow: var(--shadow);
      padding:20px;
    }
    h1{margin:0 0 10px; font-size:44px; line-height:1.08}
    @media (max-width:520px){ h1{font-size:36px} }
    h2{margin:0 0 10px; font-size:22px}
    p{margin:0; color:var(--muted); line-height:1.65}
    .subtitle{margin:0 0 18px}
    .badges{display:flex; gap:10px; flex-wrap:wrap; margin-top:12px}
    .badge{
      padding:8px 10px; border-radius:999px; font-size:13px;
      background:rgba(255,255,255,.06); border:1px solid var(--border); color:var(--muted)
    }
    .grid3{display:grid; grid-template-columns: repeat(3,1fr); gap:14px; margin:14px 0}
    @media (max-width:900px){ .grid3{grid-template-columns:1fr} }
    .list{display:grid; gap:10px; margin-top:8px}
    .item{
      display:flex; gap:10px; align-items:flex-start;
      padding:12px; border-radius:16px; border:1px solid var(--border);
      background:rgba(255,255,255,.04);
    }
    .dot{
      width:10px; height:10px; margin-top:7px; border-radius:999px;
      background: linear-gradient(135deg, var(--accent), var(--accent2));
      flex:0 0 auto;
    }
    .menu-grid{display:grid; grid-template-columns: 1fr 1fr; gap:14px}
    @media (max-width:900px){ .menu-grid{grid-template-columns:1fr} }
    .menu-item{
      display:flex; justify-content:space-between; gap:14px;
      padding:14px; border-radius:16px; border:1px solid var(--border);
      background:rgba(255,255,255,.04);
    }
    .menu-item b{display:block}
    .price{white-space:nowrap; font-weight:900; color:var(--text)}
    .small{font-size:13px; color:var(--muted)}
    .split{display:grid; grid-template-columns:1fr 1fr; gap:10px}
    @media (max-width:700px){ .split{grid-template-columns:1fr} }
    input, textarea{
      width:100%; padding:12px 14px; border-radius:14px;
      border:1px solid var(--border); background:rgba(255,255,255,.04); color:var(--text);
      outline:none;
    }
    textarea{min-height:120px; resize:vertical}
    footer{padding:30px 0; color:var(--muted); text-align:center}
  </style>
</head>

<body>
  <header>
    <div class="container nav">
      <div class="brand">
        <div class="logoWrap" title="Logo Coco Lonel">
          <!-- Mets ton fichier logo ici : logo.png -->
          <img src="logo.png" alt="Logo Coco Lonel" />
        </div>
        <div class="brandText">
          <div class="name">Coco Lonel</div>
          <div class="tagline">Restaurant • Cuisine maison</div>
        </div>
      </div>

      <nav class="links">
        <a href="#menu">Menu</a>
        <a href="#infos">Infos</a>
        <a href="#reservation">Réservation</a>
        <a href="#contact">Contact</a>
      </nav>

      <a class="btn primary" href="#reservation">Réserver</a>
    </div>
  </header>

  <main class="container hero">
    <div class="hero-grid">
      <div class="card">
        <h1>Bienvenue chez<br/>Coco Lonel</h1>
        <p class="subtitle">
          Une cuisine généreuse, des produits de saison et une ambiance chaleureuse.
          Découvrez notre menu et réservez votre table.
        </p>

        <div style="display:flex; gap:10px; flex-wrap:wrap">
          <a class="btn primary" href="#menu">Voir le menu</a>
          <a class="btn" href="#infos">Horaires & accès</a>
        </div>

        <div class="badges">
          <span class="badge">🍽️ Sur place</span>
          <span class="badge">🥡 À emporter</span>
          <span class="badge">🌿 Options végétariennes</span>
          <span class="badge">🎉 Groupes / événements</span>
        </div>
      </div>

      <div class="card">
        <h2>Infos rapides</h2>
        <div class="list">
          <div class="item"><span class="dot"></span><div><b>Adresse</b><p>12 Rue Exemple, 75000 Paris</p></div></div>
          <div class="item"><span class="dot"></span><div><b>Horaires</b><p>Mar–Dim : 12h–14h30 • 19h–22h30</p></div></div>
          <div class="item"><span class="dot"></span><div><b>Téléphone</b><p><a href="tel:+33123456789">+33 1 23 45 67 89</a></p></div></div>
        </div>
      </div>
    </div>

    <section id="menu" style="margin-top:14px">
      <div class="card">
        <h2>Menu</h2>
        <p class="small">Exemple — remplace par tes plats. (Si tu as un PDF menu, je peux te mettre un bouton “Télécharger le menu”.)</p>

        <div class="menu-grid" style="margin-top:12px">
          <div>
            <h2 style="font-size:18px; margin-top:0">Entrées</h2>
            <div class="menu-item"><div><b>Velouté de saison</b><div class="small">Crème légère, herbes fraîches</div></div><div class="price">8€</div></div>
            <div class="menu-item"><div><b>Burrata & tomates</b><div class="small">Basilic, huile d’olive</div></div><div class="price">10€</div></div>
          </div>

          <div>
            <h2 style="font-size:18px; margin-top:0">Plats</h2>
            <div class="menu-item"><div><b>Plat signature</b><div class="small">Accompagnement du jour</div></div><div class="price">18€</div></div>
            <div class="menu-item"><div><b>Option végétarienne</b><div class="small">Légumes rôtis, sauce maison</div></div><div class="price">16€</div></div>
          </div>

          <div>
            <h2 style="font-size:18px; margin-top:0">Desserts</h2>
            <div class="menu-item"><div><b>Tiramisu</b><div class="small">Café, cacao</div></div><div class="price">8€</div></div>
            <div class="menu-item"><div><b>Crème brûlée</b><div class="small">Vanille, caramel</div></div><div class="price">8€</div></div>
          </div>

          <div>
            <h2 style="font-size:18px; margin-top:0">Boissons</h2>
            <div class="menu-item"><div><b>Verre de vin</b><div class="small">Rouge / blanc / rosé</div></div><div class="price">6€</div></div>
            <div class="menu-item"><div><b>Café</b><div class="small">Espresso</div></div><div class="price">2€</div></div>
          </div>
        </div>
      </div>
    </section>

    <section id="infos" style="margin-top:14px">
      <div class="grid3">
        <div class="card">
          <h2>Horaires</h2>
          <p>
            Mar–Dim : 12h–14h30<br/>
            Mar–Dim : 19h–22h30<br/>
            <span class="small">Fermé le lundi</span>
          </p>
        </div>
        <div class="card">
          <h2>Accès</h2>
          <p>
            Métro : Ligne X (Station Y)<br/>
            Bus : 00 / 00<br/>
            <span class="small">Parking à proximité</span>
          </p>
        </div>
        <div class="card">
          <h2>Réseaux</h2>
          <p>
            Instagram : <a href="#" id="instaLink">@toncompte</a><br/>
            <span class="small">Ajoute Google Maps / avis si tu veux.</span>
          </p>
        </div>
      </div>
    </section>

    <section id="reservation" style="margin-top:14px">
      <div class="card">
        <h2>Réserver</h2>
        <p class="small">Réservation gratuite : téléphone + message.</p>

        <div style="display:flex; gap:10px; flex-wrap:wrap; margin-top:12px">
          <a class="btn primary" href="tel:+33123456789">Appeler pour réserver</a>
          <a class="btn" href="#contact">Réserver par message</a>
        </div>
      </div>
    </section>

    <section id="contact" style="margin-top:14px">
      <div class="card">
        <h2>Contact</h2>
        <p>Une question, un groupe, un événement ? Écris-nous.</p>

        <form id="contactForm">
          <div class="split">
            <input required name="name" placeholder="Ton nom" />
            <input required type="email" name="email" placeholder="Ton email" />
          </div>
          <input name="subject" placeholder="Sujet (optionnel)" />
          <textarea required name="message" placeholder="Ton message"></textarea>
          <button class="btn primary" type="submit">Envoyer</button>
          <div class="small">Le bouton ouvre ton application mail (100% gratuit, pas de serveur).</div>
        </form>
      </div>
    </section>

    <footer>
      © <span id="year"></span> Coco Lonel — Tous droits réservés
    </footer>
  </main>

  <script>
    document.getElementById("year").textContent = new Date().getFullYear();

    // Liens à personnaliser
    document.getElementById("instaLink").href = "https://instagram.com/toncompte";

    // Contact gratuit via mailto
    const form = document.getElementById("contactForm");
    form.addEventListener("submit", (e) => {
      e.preventDefault();
      const data = new FormData(form);

      const name = data.get("name");
      const email = data.get("email");
      const subject = data.get("subject") || "Demande de contact";
      const message = data.get("message");

      const to = "tonemail@example.com"; // <<< METS TON EMAIL ICI
      const body = `Nom: ${name}\nEmail: ${email}\n\nMessage:\n${message}`;
      window.location.href =
        `mailto:${to}?subject=${encodeURIComponent(subject)}&body=${encodeURIComponent(body)}`;
    });
  </script>
</body>
</html>
